# X投稿支援ツール プロンプト設計書

## 概要
本ドキュメントは、X（旧Twitter）投稿支援ツールの基本プロンプト構造を定義します。
ユーザーが投稿内容とキャラクターを選択することで、自然な投稿案を複数自動生成するシステムの設計です。

## システムアーキテクチャ

```
┌─────────────┐     ┌──────────────┐     ┌─────────────┐
│   ユーザー   │ --> │ プロンプト処理 │ --> │  LLM API   │
│   入力情報   │     │   システム    │     │            │
└─────────────┘     └──────────────┘     └─────────────┘
                            │                     │
                            └─────────────────────┘
                                      │
                            ┌─────────────────┐
                            │   投稿案生成    │
                            │  (3-5案/キャラ) │
                            └─────────────────┘
```

## 入力スキーマ

```json
{
  "input": {
    "content": "string",          // 投稿したい内容（日本語）
    "character": "string",        // 選択されたキャラクター識別子
    "options": {                  // オプション設定（拡張用）
      "tone": "string",          // トーン調整（optional）
      "hashtags": ["string"],    // 含めたいハッシュタグ（optional）
      "emoji_usage": "boolean"   // 絵文字使用有無（optional）
    }
  }
}
```

## 出力フォーマット

```json
{
  "output": {
    "character": "string",        // 使用したキャラクター
    "posts": [                    // 生成された投稿案リスト
      {
        "id": "number",          // 投稿案ID（1-5）
        "content": "string",     // 投稿内容（140文字以内）
        "char_count": "number",  // 文字数
        "features": {            // 投稿の特徴
          "has_emoji": "boolean",
          "has_hashtag": "boolean",
          "tone": "string"
        }
      }
    ],
    "metadata": {
      "generated_at": "string",  // 生成日時
      "model_version": "string"  // 使用モデルバージョン
    }
  }
}
```

## 基本プロンプトテンプレート

```text
あなたはX（旧Twitter）の投稿案を生成するアシスタントです。
以下の条件に従って、魅力的な投稿案を生成してください。

## キャラクター設定
{CHARACTER_PROMPT}

## 投稿内容
{USER_CONTENT}

## 制約条件
- 各投稿は140文字以内に収める
- 日本語で自然な表現を使用
- キャラクターの個性を反映させる
- 読みやすく、エンゲージメントを促す内容にする

## 追加指示
{ADDITIONAL_INSTRUCTIONS}

## 出力形式
以下の形式で3〜5個の投稿案を生成してください：

投稿案1:
[内容]

投稿案2:
[内容]

...（続く）
```

## プロンプト構成要素

### 1. システムロール定義
```text
あなたはX（旧Twitter）の投稿案を生成するアシスタントです。
```

### 2. キャラクター注入部（CHARACTER_PROMPT）
- 各キャラクターの詳細設定を挿入
- 口調、性格、専門知識などを定義
- 別Issueで管理される個別プロンプト

### 3. コンテンツ注入部（USER_CONTENT）
- ユーザーが入力した投稿したい内容
- そのまま挿入

### 4. 制約条件
- 140文字制限（必須）
- 日本語使用（必須）
- キャラクター反映（必須）
- エンゲージメント考慮（推奨）

### 5. 追加指示部（ADDITIONAL_INSTRUCTIONS）
オプション設定に基づく動的な指示：
- トーン指定時: 「{tone}なトーンで書いてください」
- ハッシュタグ指定時: 「以下のハッシュタグを含めてください: {hashtags}」
- 絵文字設定時: 「絵文字を{使用/不使用}してください」

## 実装例

### 入力例
```json
{
  "input": {
    "content": "今日からプログラミングの勉強を始めました。Pythonから学習していきます。",
    "character": "enthusiastic_engineer",
    "options": {
      "emoji_usage": true,
      "hashtags": ["Python", "プログラミング初心者"]
    }
  }
}
```

### プロンプト生成例
```text
あなたはX（旧Twitter）の投稿案を生成するアシスタントです。
以下の条件に従って、魅力的な投稿案を生成してください。

## キャラクター設定
あなたは熱心なエンジニアです。技術に対する情熱を持ち、
初心者にも分かりやすく説明することを心がけています。
絵文字を効果的に使い、ポジティブな雰囲気で投稿します。

## 投稿内容
今日からプログラミングの勉強を始めました。Pythonから学習していきます。

## 制約条件
- 各投稿は140文字以内に収める
- 日本語で自然な表現を使用
- キャラクターの個性を反映させる
- 読みやすく、エンゲージメントを促す内容にする

## 追加指示
- 絵文字を使用してください
- 以下のハッシュタグを含めてください: #Python #プログラミング初心者

## 出力形式
以下の形式で3〜5個の投稿案を生成してください：

投稿案1:
[内容]

投稿案2:
[内容]

...（続く）
```

## 拡張性の考慮

### 将来的な拡張ポイント
1. **コンテキスト情報の追加**
   - 時間帯による投稿最適化
   - トレンド情報の反映
   - 過去の投稿履歴の参照

2. **高度な制約条件**
   - URL短縮の考慮
   - 画像添付時の文字数調整
   - リプライ/引用RT対応

3. **評価メトリクス**
   - 読みやすさスコア
   - エンゲージメント予測
   - キャラクター一貫性チェック

## バージョン管理

- v1.0.0: 初期設計（本ドキュメント）
- 今後: キャラクター別プロンプトの追加に応じてマイナーバージョンアップ

## 関連ドキュメント

- キャラクター別プロンプト設計（別Issue管理）
- API実装仕様（今後作成予定）
- ユーザーインターフェース設計（今後作成予定）